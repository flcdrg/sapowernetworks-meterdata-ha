[project]
name = "sapowernetworks-meterdata-ha"
version = "0.1.0"
requires-python = ">=3.14.2"

[dependency-groups]
dev = [
    "colorlog>=6.11.0",
    "pytest-homeassistant-custom-component>=0.13.346",
    "ruff>=0.15.22",
]

[tool.uv]
package = false
